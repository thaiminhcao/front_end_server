// import React from 'react';
import Layout from '../dashboard';

const Index = () => {
    return (
        <div>
            <Layout />
            {/* Nội dung của trang Profile */}
        </div>
    );
};

export default Index;
